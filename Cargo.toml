[package]
name = "shooty"
version = "0.1.0"
edition = "2024"
default-run = "shooty"

[dependencies]
bevy = { version = "0.19.1", features = ["jpeg"] }
rand = "0.8"
serde = { version = "1", features = ["derive"] }
bincode = "1.3"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
